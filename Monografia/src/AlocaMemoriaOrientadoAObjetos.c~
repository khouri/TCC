/*Aloca memória para doubles.*/
double * alocaMemoriaDouble(int tamanhoAlocar){

    return (double*)malloc(tamanhoAlocar*sizeof(double));
}

/*Libera memoria do vetor de double.*/
void liberaMemoriaDouble(double * liberar){

    free(liberar);
}
